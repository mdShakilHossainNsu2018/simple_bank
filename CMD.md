# Database migration
```shell
$ migrate create -ext sql -dir db/migrations -seq <migration_name>
```

# Setup mockgen
```shell
vi ~/.zshrc
i
export PATH=$PATH:~/go/bin
```
press esc
wq
```shell
source ~/.zshrc
which mockgen
```
